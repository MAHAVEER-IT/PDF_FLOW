/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration for handling large file uploads
  // Size limit is now set in the API routes themselves
};

export default nextConfig;
